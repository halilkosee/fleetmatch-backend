spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fleetmatch_local}
    username: ${DB_USERNAME:fleetmatch_local}
    password: ${DB_PASSWORD:fleetmatch_local}

  jpa:
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: ${JPA_FORMAT_SQL:true}

fleetmatch:
  environment: LOCAL
  jwt:
    secret: ${JWT_SECRET:local-development-jwt-secret-change-me-64-characters-minimum-1234567890}
  api:
    base-url: ${API_BASE_URL:http://localhost:8080}
  frontend:
    base-url: ${FRONTEND_BASE_URL:http://localhost:5173}
